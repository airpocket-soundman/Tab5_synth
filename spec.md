# Tab5 シンセ仕様

## 概要

このプロジェクトは、M5Stack Tab5 上で動作するモジュラーシンセ風の楽器を目指す。
本体は横画面で使用し、操作の中心はタッチディスプレイとする。

## 現在の製品方針

- タッチ UI 上でモジュラーシンセの雰囲気を再現する
- 波形を選択できる
- 音作り用のスイッチやスライダーを用意する
- 画面右下に正方形の XY パッド領域を設ける
- XY パッドでは片軸で音程、もう片軸で別のパラメータを操作する
- ハードウェアと UI が成立するなら和音入力にも対応する
- 複数の LFO を用意する
- 任意のパラメータにモジュレーションを割り当てられるようにする

## 実装方針

拡張しやすさのため、主要機能はクラス単位で分割する。

- main.cpp には初期化とメインループだけを残す
- 音源制御は専用クラスに分ける
- 画面描画とレイアウトは専用クラスに分ける
- タッチ入力とアプリ状態管理はアプリ制御クラスに集約する
- 将来の LFO、フィルタ、エフェクトも同じ方針でモジュール化する

## ハードウェアメモ

- 対象デバイス: M5Stack Tab5
- タッチディスプレイ: マルチタッチ対応
- Tab5 の公式情報ベースでは最大 5 点タッチ対応を想定する

## 初期 UI コンセプト

横画面内で、音作りエリアと演奏エリアを明確に分ける。

- 左側: 波形選択ボタン
- 中央: フィルタ、エフェクト、アンプ関連のスライダーやスイッチ
- 右上: 現在値表示や将来的なプリセット領域
- 右下: 表現用の正方形 XY パッド

## フェーズ 1 MVP

最初の実装は、まず音が出せる最小構成まで絞る。

レイアウト:

- 画面左半分: 波形選択 UI
- 画面右半分: 演奏用タッチ領域

動作:

- 左側で波形を 1 つ選ぶ
- 右側をタッチすると音が鳴る
- タッチ位置で音程を決める
- この段階では波形と音程だけを扱う
- フィルタ、エンベロープ、エフェクト、モジュレーションの UI はフェーズ 1 では入れない

目的:

- 低遅延で音が出るか確認する
- Tab5 のタッチ操作を検証する
- 後続のモジュラー機能追加前に、演奏の基本操作を固める

## 音源エンジンの方向性

操作感は即時性重視だが、音源の方向性は固定配線ではなくモジュラー寄りとする。

- 波形選択付きのオシレータ
- アナログシンセ風の音作り
- タッチ中心のライブ演奏フロー
- 指の移動に対する高速な追従
- 複数のモジュレーションソース
- ユーザーが選べるモジュレーションルーティング

## モジュラー方針

自由にパッチケーブルを引く完全再現ではなく、5 インチ画面でも扱いやすいモジュラー風エミュレータを目指す。

設計意図:

- 主要な機能をパネルまたはブロックとして見せる
- UI 上でモジュレーション元と送り先を選べるようにする
- ライブ演奏中でも素早くルーティングできるようにする
- 画面が狭くなりすぎる複雑な配線操作は避ける

## 候補となるオシレータ波形

- Sine
- Saw
- Square
- Triangle

## 候補となる機能ブロック

- オシレータ波形選択
- フィルタ
- アンプ制御
- エフェクト
- LFO
- モジュレーションルーティング

## モジュレーション機能

複数の LFO を同時に動かし、任意のパラメータへ割り当てられるようにする。

基本要件:

- 複数の LFO を同時に動作させられる
- 各 LFO に個別の Rate と Depth を持たせる
- 各 LFO の波形を選択できる
- 各 LFO を 1 つ以上の送り先に割り当てられる
- 送り先の設定は UI から変更できる

候補となる LFO 波形:

- sine
- triangle
- square
- sample and hold

候補となる LFO パラメータ:

- rate
- depth
- phase
- sync の on/off
- retrigger 動作

候補となるモジュレーション送り先:

- オシレータの音程
- オシレータの fine pitch
- フィルタ cutoff
- resonance
- amp level
- pan
- effect mix
- delay time
- reverb amount
- XY パッド Y 軸の割り当て量

## ルーティングモデル

モジュラーらしい柔軟性は持たせるが、自由配線の描画は必須にしない。

現時点での有力案:

- モジュレーション元を選ぶ
- 送り先を選ぶ
- amount を設定する
- 複数ルートを同時に持てるようにする

実装候補:

- マトリクス形式のルーティング表
- モジュレーション元ごとの設定パネル
- 送り先ごとの設定パネル

5 インチ画面では、モジュラーらしさと密度のバランスを考えるとマトリクス形式が有力。

## XY パッド

画面右下に正方形のタッチ領域を用意する。

候補となる割り当て:

- X 軸: 音程
- Y 軸: filter cutoff

将来的に検討する Y 軸候補:

- volume
- effect depth
- modulation amount

注記:

- XY パッド自体は製品全体の中心機能として維持する
- ただしフェーズ 1 では、まず右半分全体を使う大きな演奏領域に簡略化する

## ポリフォニー / 和音

この製品は和音演奏に対応したい。

現時点のメモ:

- Tab5 の公式情報では最大 5 点タッチが案内されている
- タッチ点数が多くても、演奏しやすさがそのまま保証されるわけではない
- 実用上の初期目標は 3 音から 4 音程度の安定した和音入力が妥当

フェーズ 1 では:

- まず単音、または単純なモノフォニック操作から始める
- 音程割り当てと反応速度を先に検証する
- 和音はベースの演奏体験が安定した後で追加する

## フェーズ 1 詳細

### 波形選択

波形選択エリアは画面左側に配置する。

フェーズ 1 の候補波形:

- sine
- saw
- square
- triangle

期待する動作:

- 常に 1 つだけ有効な波形を選ぶ
- 選択中の波形は視覚的に強調表示する
- 波形変更は新しく鳴る音へ即時反映する

### 演奏タッチ領域

演奏タッチ領域は画面右半分全体を使う。

フェーズ 1 の割り当て:

- 横方向を音程に割り当てる
- 縦方向は未使用、または将来拡張用に予約する

音程割り当ての候補:

- 幅全体を連続音程として使う
- 幅全体を半音単位に量子化して使う

フェーズ 1 の有力案:

- 楽器として評価しやすいため、半音単位の量子化を優先する

期待する動作:

- touch down で発音開始
- 指を動かすとリアルタイムで音程更新
- 指を離すと発音停止

### 試作段階の実装メモ

現在の試作では、まず演奏体験の確認を優先する。

- タッチ更新ごとの全画面再描画は行わない
- 状態表示とタッチマーカーのみ部分更新する
- 演奏領域の縦線ガイドは一旦外す
- 音切れを避けるため、試作段階ではバックグラウンドで連続生成する音源バッファ方式を使う
- 波形差し替えは AudioEngine の連続 PCM 生成で扱う

## 未決定事項

- 音入力は鍵盤型、パッド型、XY パッド型のどれを主に使うか
- XY パッドは単音専用にするか、別領域で和音を扱うか
- 初期バージョンに含めるエフェクトは何か
- 初期バージョンでプリセット保存を入れるか
- v1 でどこまでエンベロープを触れるようにするか
- v1 で LFO を何基にするか
- 1 つのモジュレーション元を複数送り先へ送れるようにするか
- 1 つの送り先が複数モジュレーション元を受けられるようにするか
- 専用のモジュレーションマトリクス画面を作るか
- フェーズ 1 の音程は連続にするか量子化するか
- フェーズ 1 の右側演奏領域は何オクターブ分を扱うか

## 決定ログ

### 2026-04-03

- M5Stack Tab5 向けのタッチ操作中心シンセを作る方針を決定
- 横画面で使用する方針を決定
- 右下に正方形 XY パッドを置く案を中核 UI として採用
- 和音対応を目標に含める
- Tab5 のタッチ能力は最大 5 点を前提とするメモを追加
- 単純なアナログ風シンセから、モジュラーシンセ風エミュレータへ方向性を更新
- 複数 LFO とユーザー設定可能なモジュレーションルーティングをコア要件に追加
- フェーズ 1 は左に波形選択、右に音程操作用タッチ領域の最小構成に絞る
- フェーズ 1 はまず波形選択と音程だけで発音できる状態を目標にする
- 実装の最初の試作は PlatformIO + Arduino + M5Unified 前提で進める
- 初期コードは単音、右半分タッチ演奏、左半分波形選択で構成する
- フェーズ 1 の音程は右半分の X 座標を半音単位に量子化して割り当てる
- ツールチェーン解決の問題に対して、一時的に extra_script で RISC-V コンパイラの絶対パスを差し込む
- 表示のちらつきと音切れ対策として、試作段階では部分描画と連続発音を優先する
- 実装コードは main.cpp へ詰め込まず、音源、UI、アプリ制御をクラス分割して拡張しやすく保つ

