# Tab5 シンセ仕様

## 概要

このプロジェクトは、M5Stack Tab5 上で動作するモジュラーシンセ風の楽器を目指す。
本体は横画面で使用し、操作の中心はタッチディスプレイとする。

## 現在の製品方針

- タッチ UI 上でモジュラーシンセの雰囲気を再現する
- 波形を選択できる
- 音作り用のスイッチやスライダーを用意する
- 画面右下に正方形の XY パッド領域を設ける
- XY パッドでは片軸で音程、もう片軸で別のパラメータを操作する
- ハードウェアと UI が成立するなら和音入力にも対応する
- 複数の LFO を用意する
- 任意のパラメータにモジュレーションを割り当てられるようにする

## 実装方針

拡張しやすさのため、主要機能はクラス単位で分割する。

- main.cpp には初期化とメインループだけを残す
- 音源制御は専用クラスに分ける
- 画面描画とレイアウトは専用クラスに分ける
- タッチ入力とアプリ状態管理はアプリ制御クラスに集約する
- 将来の LFO、フィルタ、エフェクトも同じ方針でモジュール化する

## 設定方針

チューニングしやすさのため、固定値は Config に集約する。

- 音作りに関する係数は AudioConfig で管理する
- UI のサイズ、余白、色、タッチパッド比率は UiConfig で管理する
- 波形ごとの音量補正も Config から変更できるようにする
- タッチパッド上の座標表示はエリア内の相対座標として扱う

## ハードウェアメモ

- 対象デバイス: M5Stack Tab5
- タッチディスプレイ: マルチタッチ対応
- Tab5 の公式情報ベースでは最大 5 点タッチ対応を想定する
- Tab5 は ES8388 オーディオ codec と ES7210 AEC フロントエンドを搭載する
- オンボードマイクはデュアルマイク構成
- M5Unified の Mic サンプルでは、マイクとスピーカーは同時利用しない前提になっている

参照:

- [Tab5 製品仕様](https://docs.m5stack.com/en/core/Tab5)
- [Tab5 Mic API / サンプル](https://docs.m5stack.com/en/arduino/m5tab5/mic)

## オーディオ入出力と GPIO

### オンボードマイク系

Tab5 のオンボードマイクは ES7210 を通して接続されている。
この部分の信号線は基板上で固定されているため、ソフト側ではこの配線を前提に扱う。

固定ピン:

- `G30`: MCLK
- `G27`: SCLK / BCLK
- `G28`: ASDOUT
- `G29`: LRCK / WS
- `G32`: I2C SCL
- `G31`: I2C SDA

注意:

- これは Tab5 公式 PinMap の ES7210 欄に基づく
- オンボードマイク入力は任意の GPIO に変更しない
- 実装ではこの固定配線を使う前提で `OnboardMicSource` を組む

### 外部 I2S 入力系

外部 I2S は M5-Bus 上の空き GPIO を使って受ける。
公式に「外部 I2S 用の固定配線」があるわけではないため、以下は本プロジェクトでの割り当て案とする。
これは公式仕様の転載ではなく、M5-Bus の空き GPIO を使った設計上の提案である。

採用案:

- `G16`: 外部 I2S BCLK
- `G45`: 外部 I2S LRCK / WS
- `G3`: 外部 I2S DIN
- `G4`: 外部 I2S MCLK 予備または必要時のみ使用

理由:

- M5-Bus 上で比較的扱いやすい GPIO である
- 既存のオンボードオーディオ配線とは分離できる
- RX 専用なら最低限 `BCLK / LRCK / DIN` で開始できる
- 必要な外部デバイスのみ `MCLK` を使う形にできる

注意:

- `G16 / G45 / G3 / G4` は本プロジェクト側の提案割り当て
- 実装時は外部 I2S デバイスの `MCLK` 必須有無を確認する
- 将来的に他の M5-Bus 拡張と同時利用する場合は競合確認が必要

参照:

- [Tab5 PinMap](https://docs.m5stack.com/en/core/Tab5?id=PinMap)

## 初期 UI コンセプト

横画面内で、音作りエリアと演奏エリアを明確に分ける。

- 左側: 音源選択アイコン列とコンパクトなボリュームスライダー
- 右側: 演奏用タッチパッド
- タッチパッド内: 中央基準の破線ガイドと左下の座標表示

## フェーズ 1 MVP

最初の実装は、まず音が出せる最小構成まで絞る。

レイアウト:

- 左側: 音源選択アイコンとボリュームコントローラ
- 右側: 演奏用タッチ領域
- 左右の幅配分は UiConfig で調整できるようにする

動作:

- 左上に 6 個の音源アイコンを横一列で均等に配置する
- アイコン構成は `Sine / Saw / Square / Triangle / MIC / I2S`
- `OSC / MIC / I2S` の別ボタンは持たない
- 4 つの波形アイコンを押した場合は `OSC` ソースを選び、対応する波形を選ぶ
- `MIC` アイコンを押した場合はオンボードマイク入力を選ぶ
- `I2S` アイコンを押した場合は外部 I2S 入力を選ぶ
- ボリュームコントローラはアイコン列の下に置く
- ボリューム欄はスライダー中心の小さなカラムにする
- ボリューム値はスライダー横に数値のみ表示する
- 右側をタッチすると音が鳴る
- タッチ位置で音程を決める
- タッチパッド内に x=0 と y=0 の破線ガイドを表示する
- 破線ガイドは縦横それぞれ 1 本ずつ、エリア全体を貫く形にする
- 座標表示は枠で囲わず、タッチパッド左下に横並びで小さく表示する
- 座標表示は `x = -000, y = -000` 形式とする
- 座標表示は演奏領域の中央を 0 とし、X/Y ともに -100 から 100 で正規化する
- タッチパッドのサイズを変えても座標表示は相対基準を維持する
- この段階では波形または入力ソース、音程、ボリュームを扱う
- フィルタ、エンベロープ、エフェクト、モジュレーションの UI はフェーズ 1 では入れない

目的:

- 低遅延で音が出るか確認する
- Tab5 のタッチ操作を検証する
- 後続のモジュラー機能追加前に、演奏の基本操作を固める

## 音源エンジンの方向性

操作感は即時性重視だが、音源の方向性は固定配線ではなくモジュラー寄りとする。

- 波形選択付きのオシレータ
- アナログシンセ風の音作り
- タッチ中心のライブ演奏フロー
- 指の移動に対する高速な追従
- 複数のモジュレーションソース
- ユーザーが選べるモジュレーションルーティング

## VCO 方針

VCF を作り始める前に、まず VCO 相当の音源部を完成させる。

初期の音源ソース候補:

- 内部波形オシレータ
- オンボードマイク入力
- 外部 I2S 入力

VCO セレクタの方針:

- 音源ソース切替は独立ボタンではなく、6 個のアイコン列に統合する
- 4 つの波形は内部オシレータの各波形選択を兼ねる
- `MIC` と `I2S` は入力ソース選択を兼ねる
- フェーズ 1 では同時ミックスではなく選択式を基本とする
- 将来的な同時ミックスに備えて、内部では共通のオーディオバッファ形式で扱う

VCO ソースごとの想定:

- 内部波形オシレータ: 既存の Sine / Saw / Square / Triangle
- オンボードマイク: Tab5 内蔵デュアルマイク入力
- 外部 I2S: 別基板からのデジタル音声入力

設計上の注意:

- オンボードマイクは M5Unified 高レベル API のままだとスピーカー同時利用に制約がある
- 本格的に VCO ソースとして扱うなら、入力と出力を同じオーディオエンジンで管理する必要がある
- 外部 I2S も将来の VCF / VCA / FX に通すため、早い段階で内部バッファへ統一する

## 入力ソース仕様

### オンボードマイク

- Tab5 の内蔵マイクを音源ソースとして使う
- 初期段階ではモニター入力として扱う
- 将来的には VCF / VCA / エフェクトの入力へ流せるようにする

### 外部 I2S

- 別基板の I2S 出力を Tab5 側で受ける
- GPIO 割り当て案は `G16 / G45 / G3 / G4`
- 初期段階ではモノラルまたはステレオのどちらかに統一して内部で扱う
- 将来的にはオンボードマイクと同様にフィルタやエフェクトへ送る

### 共通入力バッファ

- 内部波形、オンボードマイク、外部 I2S を共通のサンプルフォーマットへ変換する
- 各ソースは AudioEngine 内で共通の入力スロットとして扱う
- VCO 完成後は、この共通バッファを VCF へ渡す

## VCF 前提の段階分け

### 段階 1

- 内部波形オシレータを安定化する
- 音量差と UI を調整する

### 段階 2

- `Sine / Saw / Square / Triangle / MIC / I2S` の 6 アイコン列を完成させる
- アイコン押下で音源ソース切替を行えるようにする

### 段階 3

- オンボードマイク入力を AudioEngine に統合する
- 外部 I2S 入力を AudioEngine に統合する
- 各入力を内部共通バッファへそろえる

### 段階 4

- VCO 部を完成とみなし、VCF の設計と実装に入る
- 最初の VCF は LPF を優先する

## 現在の実装状況

- `AudioSource` 抽象クラスを追加済み
- `OscillatorSource` を既存の発音コードから分離済み
- `OnboardMicSource` と `ExternalI2SSource` はクラスだけ作成済み
- 現在は `OscillatorSource` のみ実際に発音可能
- `AudioEngine` は音源ソース切替対応済み
- UI は `Sine / Saw / Square / Triangle / MIC / I2S` の 6 アイコン列へ移行済み
- `MIC / I2S` の実入力統合は次段階

## モジュラー方針

自由にパッチケーブルを引く完全再現ではなく、5 インチ画面でも扱いやすいモジュラー風エミュレータを目指す。

設計意図:

- 主要な機能をパネルまたはブロックとして見せる
- UI 上でモジュレーション元と送り先を選べるようにする
- ライブ演奏中でも素早くルーティングできるようにする
- 画面が狭くなりすぎる複雑な配線操作は避ける

## 候補となるオシレータ波形

- Sine
- Saw
- Square
- Triangle

## 候補となる機能ブロック

- オシレータ波形選択
- フィルタ
- アンプ制御
- エフェクト
- LFO
- モジュレーションルーティング

## モジュレーション機能

複数の LFO を同時に動かし、任意のパラメータへ割り当てられるようにする。

基本要件:

- 複数の LFO を同時に動作させられる
- 各 LFO に個別の Rate と Depth を持たせる
- 各 LFO の波形を選択できる
- 各 LFO を 1 つ以上の送り先に割り当てられる
- 送り先の設定は UI から変更できる

候補となる LFO 波形:

- sine
- triangle
- square
- sample and hold

候補となる LFO パラメータ:

- rate
- depth
- phase
- sync の on/off
- retrigger 動作

候補となるモジュレーション送り先:

- オシレータの音程
- オシレータの fine pitch
- フィルタ cutoff
- resonance
- amp level
- pan
- effect mix
- delay time
- reverb amount
- XY パッド Y 軸の割り当て量

## ルーティングモデル

モジュラーらしい柔軟性は持たせるが、自由配線の描画は必須にしない。

現時点での有力案:

- モジュレーション元を選ぶ
- 送り先を選ぶ
- amount を設定する
- 複数ルートを同時に持てるようにする

実装候補:

- マトリクス形式のルーティング表
- モジュレーション元ごとの設定パネル
- 送り先ごとの設定パネル

5 インチ画面では、モジュラーらしさと密度のバランスを考えるとマトリクス形式が有力。

## XY パッド

画面右下に正方形のタッチ領域を用意する。

候補となる割り当て:

- X 軸: 音程
- Y 軸: filter cutoff

将来的に検討する Y 軸候補:

- volume
- effect depth
- modulation amount

注記:

- XY パッド自体は製品全体の中心機能として維持する
- ただしフェーズ 1 では、まず右側の大きな演奏領域に簡略化する

## ポリフォニー / 和音

この製品は和音演奏に対応したい。

現時点のメモ:

- Tab5 の公式情報では最大 5 点タッチが案内されている
- タッチ点数が多くても、演奏しやすさがそのまま保証されるわけではない
- 実用上の初期目標は 3 音から 4 音程度の安定した和音入力が妥当

フェーズ 1 では:

- まず単音、または単純なモノフォニック操作から始める
- 音程割り当てと反応速度を先に検証する
- 和音はベースの演奏体験が安定した後で追加する

## 未決定事項

- 音入力は鍵盤型、パッド型、XY パッド型のどれを主に使うか
- XY パッドは単音専用にするか、別領域で和音を扱うか
- 初期バージョンに含めるエフェクトは何か
- 初期バージョンでプリセット保存を入れるか
- v1 でどこまでエンベロープを触れるようにするか
- v1 で LFO を何基にするか
- 1 つのモジュレーション元を複数送り先へ送れるようにするか
- 1 つの送り先が複数モジュレーション元を受けられるようにするか
- 専用のモジュレーションマトリクス画面を作るか
- フェーズ 1 の音程は連続にするか量子化するか
- フェーズ 1 の右側演奏領域は何オクターブ分を扱うか
- オンボードマイクと外部 I2S を将来ミックス可能にするか
- 外部 I2S をモノラル固定にするか、ステレオ入力を扱うか

## 決定ログ

### 2026-04-03

- M5Stack Tab5 向けのタッチ操作中心シンセを作る方針を決定
- 横画面で使用する方針を決定
- 右下に正方形 XY パッドを置く案を中核 UI として採用
- 和音対応を目標に含める
- Tab5 のタッチ能力は最大 5 点を前提とするメモを追加
- 単純なアナログ風シンセから、モジュラーシンセ風エミュレータへ方向性を更新
- 複数 LFO とユーザー設定可能なモジュレーションルーティングをコア要件に追加
- フェーズ 1 は左に波形選択、右に音程操作用タッチ領域の最小構成に絞る
- フェーズ 1 はまず波形選択と音程だけで発音できる状態を目標にする
- 実装の最初の試作は PlatformIO + Arduino + M5Unified 前提で進める
- 初期コードは単音、右側タッチ演奏、左側波形選択で構成する
- TRIANGLE の下にボリュームコントローラを追加する
- フェーズ 1 の音程は右側演奏領域の X 座標を半音単位に量子化して割り当てる
- ツールチェーン解決の問題に対して、一時的に extra_script で RISC-V コンパイラの絶対パスを差し込む
- 表示のちらつきと音切れ対策として、試作段階では部分描画と連続発音を優先する
- 実装コードは main.cpp へ詰め込まず、音源、UI、アプリ制御をクラス分割して拡張しやすく保つ
- 音作りに関する係数は Config から調整できるようにする
- UI のサイズ、余白、色も Config から調整できるようにする
- 波形ボタンは 4 つ横並びのアイコンのみ表示にする
- 右側タッチパッド上の情報表示は座標表示のみに絞る
- タッチパッドの座標表示はエリア基準の相対座標として維持する
- タッチパッドの幅やガイド線設定も UiConfig から変更できるようにする
- タッチパッド中央の x=0, y=0 は縦横 1 本ずつの破線で表示する
- 座標値は枠なしでタッチパッド左下へ小さく表示する
- 座標値は `x = -000, y = -000` の横並び表示にする
- ボリューム欄はスライダー中心の小型カラムにし、数値は横にだけ表示する
- VCO は内部波形、オンボードマイク、外部 I2S を切り替え可能な構成へ拡張する
- VCO 完成後に VCF 実装へ進む
- AudioEngine は `AudioSource` 抽象化ベースへ移行する
- `OscillatorSource` を既存発音コードから分離する
- `OnboardMicSource` と `ExternalI2SSource` はクラス骨格を先に追加する
- UI の左上は `Sine / Saw / Square / Triangle / MIC / I2S` の 6 アイコンを均等に並べる
- `OSC / MIC / I2S` の別選択ボタンは廃止する
- 外部 I2S の GPIO 割り当て案は `G16 / G45 / G3 / G4` とする
- 外部 I2S は `I2S_NUM_1` の slave RX として実装し、`BCLK=G16 / WS=G45 / DIN=G3` を使用する
- 外部 I2S の MCLK は現段階では未使用とし、`G4` は将来拡張用に予約する
- 外部 I2S は入力バッファを `M5.Speaker` へ順次流すモニター方式で動作させる
- オンボード MIC は Tab5 の内蔵スピーカーと同一音声系統を共有するため、現段階では選択 UI のみで実入力切替は未完とする
- オンボード MIC はリアルタイム VCO ではなく、PSRAM へ最大 5 秒録音してサンプル音源として使う方式へ変更する
- `MIC` アイコンを押したときは、その場で再録音して最新サンプルを `OnboardMicSource` に差し替える
- 録音サンプルはモノラル 16bit / 16kHz を既定とし、再生時は MIDI ノート差分でサンプルレートを変えてピッチを合わせる
- タッチパッドのドット描画範囲は一部エリアではなく、タッチパッド全域とする
- タッチパッドは最大 5 点のマルチタッチ入力を受け付け、各タッチ位置にドットを同時表示する
- `OSC` と `MICサンプル` は仮想チャンネルを使って最大 5 ボイスの同時発音に対応する
- マルチタッチ対応後は XY 座標の数値表示を廃止し、タッチ位置はドット表示のみとする
- タッチパッド更新は全面再描画ではなく、前回ドットの消去と新規ドット描画だけで差分更新する
- タッチパッドは右側全面ではなく、右エリア内の正方形パッドとして配置する
- タッチパッドサイズは右エリアに対しておよそ 2/3 程度まで縮小する
- 正方形タッチパッドの下に `SEMI / CONT` の切替スイッチを置き、半音階量子化と連続音程を切り替えられるようにする

## 現在の実装メモ

### UI

- 画面は 180 度回転を既定にしている
- 左上に `Sine / Saw / Square / Triangle / MIC / I2S` の 6 音源アイコンを配置している
- その下にパラメータアイコン列を置いている
- パラメータアイコンは `VOL / ATK / DEC / SUS / REL` の 5 個
- パラメータアイコンは値表示を兼ねた縦バー表示にしている
- 選択中のパラメータだけを下部の共通スライダーで変更する
- 共通スライダーは `SEMI / CONT` の下にあり、幅はタッチパッド幅に合わせている
- 右側は正方形のタッチパッドで、最大 5 点のマルチタッチ入力を受け付ける
- タッチ位置は円マーカーで表示し、差分更新で描画する
- タッチパッド中央には縦横 1 本ずつの破線ガイドを表示する

### 音源

- `OscillatorSource` は `Sine / Saw / Square / Triangle` の発音に対応している
- `OnboardMicSource` はリアルタイム入力ではなく、録音済みサンプルの再生音源として使っている
- `ExternalI2SSource` は外部 I2S 入力のモニター再生に対応している
- 波形ごとの音量差は `SynthConfig` の `peak` と `trim` で補正できる
- 現状の低音量感は音程別補正を入れておらず、小型スピーカー特性の影響を受ける

### MIC サンプラー

- `MIC` アイコンを押している間だけ録音する
- 指を離した時点で録音を終了し、直近の録音をサンプル音源として使う
- 押下時間が 1 秒未満の場合は新録音を破棄し、前回のサンプルを保持する
- 録音先は SD ではなく PSRAM 上の `SampleBuffer` を使う
- 録音上限は既定で 5 秒
- 録音後は無音トリム、DC オフセット除去、正規化、フェード処理を行う
- 録音済みサンプルはタッチパッドの音程に応じて再生レートを変えて鳴らす

### エンベロープジェネレータ

- `EnvelopeGenerator` クラスを追加済み
- 現在は `AMP ADSR` として使用している
- 各ボイスごとに独立した ADSR を持つ
- `ATK / DEC / SUS / REL` は共通スライダーで変更できる
- `Release` は離鍵後の減衰として動作する
- `Attack / Decay / Release` の最大時間と初期値は `SynthConfig` で管理する

### 実装クラス

- `SynthApp`: タッチ入力、状態同期、録音開始終了の制御
- `PerformanceUi`: レイアウト、アイコン描画、タッチパッド描画
- `ParameterIcon`: パラメータアイコンのバー描画
- `AudioEngine`: 音源切替、ボイス管理、ADSR 更新
- `EnvelopeGenerator`: 各ボイスの ADSR 計算
- `OscillatorSource`: 内部波形音源
- `OnboardMicSource`: MIC 録音とサンプル再生
- `ExternalI2SSource`: 外部 I2S モニター入力
- `SampleBuffer`: PSRAM 上の録音バッファ

### 現在の制約

- `Y` 軸はまだ音作りパラメータに割り当てていない
- `VCF` は未実装
- `MIC` はライブ入力 VCO ではなく、サンプル録音音源としてのみ使う
- 外部 I2S は現状モニター用途で、音程追従やフィルタ入力の統合は未完
- 音程ごとの聴感補正は未実装

### 次段候補

- `VCF` の追加
- `Y` 軸へのパラメータ割り当て
- 音程別ラウドネス補正
- `VCF EG` の追加
- LFO とモジュレーションルーティングの追加

### 2026-04-06

- `VOL` アイコンを値表示付きのバー型パラメータアイコンへ変更した
- `VOL / ATK / DEC / SUS / REL` の 5 パラメータを共通スライダーで編集する UI に変更した
- `ParameterIcon` クラスを追加し、パラメータ描画を専用クラスへ分離した
- `AMP ADSR` 用の `EnvelopeGenerator` クラスを追加した
- `AudioEngine` は各ボイスごとに ADSR を持ち、音量へ適用する構成へ変更した
- `MIC` 録音は押している間だけ行い、1 秒未満なら前回サンプルを保持する仕様にした
- タッチパッドの丸マーカー消去時に外枠と中央ガイドを再描画するように修正した

## LFO モジュレーションマトリクス（案）

### LFO 側パラメータ

本プロジェクトで扱う LFO パラメータは以下の 3 つです。

- `Rate`: 揺れの速さ
- `Depth`: 揺れの深さ（変調量）
- `Shape`: 揺れ方の形（sine / triangle / square / sample-hold など）

### 対象パラメータ一覧と意味

#### A. 現在コード上に存在するパラメータ（UI/state 実装済み）

| カテゴリ | パラメータ | 意味 |
|---|---|---|
| AMP | Volume | 音量（最終出力レベル） |
| AMP EG | Attack | ノートオンから最大音量までの立ち上がり時間 |
| AMP EG | Decay | 最大音量からサステインレベルまで下がる時間 |
| AMP EG | Sustain | キー押下中に維持する音量 |
| AMP EG | Release | ノートオフから無音になるまでの時間 |
| Delay | Delay Time | ディレイの遅延時間 |
| Delay | Delay Feedback | 反復量（残響の繰り返しの強さ） |
| Delay | Delay Mix | Dry/Wet バランス |
| Chorus | Chorus Rate | コーラス揺れの速さ |
| Chorus | Chorus Depth | コーラス揺れの深さ |
| Chorus | Chorus Mix | Dry/Wet バランス |
| Filter | Filter Cutoff | フィルタのカットオフ周波数 |
| Filter | Filter Resonance | カットオフ近傍の強調量（Q） |
| Filter | Filter Mix | フィルタ処理の混合量 |

#### B. 潜在的な対象（未実装）

| カテゴリ | パラメータ | 意味 |
|---|---|---|
| OSC | Pitch | 基本ピッチ（浅くかけるとビブラート） |
| OSC | Fine Tune | ピッチ微調整 |
| OSC | Portamento Time | ノート間スライド時間 |
| OSC | PWM Duty | パルス波デューティ比 |
| OSC | Wave Morph | 波形間の連続モーフィング量 |
| Mix | Pan | ステレオ定位 |
| AMP | Tremolo Depth | 音量揺れ（AM）の深さ |
| Filter | Filter Drive | フィルタ入力のドライブ量 |
| FX Router | Effect On/Off | エフェクトブロックのオンオフ制御 |
| Delay | Delay Time Sync | テンポ同期倍率 |
| Chorus | Chorus Phase/Delay | コーラス内部位相差 / プリディレイ |
| Mod | LFO Amount (per route) | ルーティングごとの変調量 |
| Mod | LFO Rate (meta) | LFO 速度そのものの変調 |
| Mod | LFO Shape (meta) | LFO 波形形状そのものの変調 |

### 対象ごとの LFO パラメータ有効性

基本方針:

- 連続量パラメータは `Rate / Depth / Shape` の 3 つすべてが有効。
- On/Off のような二値対象は、主に `Rate` と `Shape` が有効。`Depth` はしきい値やスイング量として使う場合のみ有効。
- メタ変調（`LFO Rate` や `LFO Shape`）は、2 系統目の変調源や拡張アーキテクチャがある場合に有効。

| 対象パラメータ | Rate | Depth | Shape | 有効パラメータ数 | 補足 |
|---|---|---|---|---|---|
| Volume | Y | Y | Y | 3 | トレモロ的な制御 |
| Attack | Y | Y | Y | 3 | エンベロープ速度の周期変化 |
| Decay | Y | Y | Y | 3 | エンベロープ速度の周期変化 |
| Sustain | Y | Y | Y | 3 | サステインレベル変化 |
| Release | Y | Y | Y | 3 | リリース時間の周期変化 |
| Delay Time | Y | Y | Y | 3 | ピッチ感を伴う時間揺れ |
| Delay Feedback | Y | Y | Y | 3 | 反復量の変化 |
| Delay Mix | Y | Y | Y | 3 | Wet 量の変化 |
| Chorus Rate | Y | Y | Y | 3 | コーラス速度の変化 |
| Chorus Depth | Y | Y | Y | 3 | コーラス深さの変化 |
| Chorus Mix | Y | Y | Y | 3 | Wet 量の変化 |
| Filter Cutoff | Y | Y | Y | 3 | 定番のフィルタLFO |
| Filter Resonance | Y | Y | Y | 3 | レゾナンスの動的変化 |
| Filter Mix | Y | Y | Y | 3 | ブレンド量の変化 |
| Pitch | Y | Y | Y | 3 | ビブラート / 階段状ピッチ変調 |
| Fine Tune | Y | Y | Y | 3 | 微細なビブラート |
| Portamento Time | Y | Y | Y | 3 | グライド時間の変化 |
| PWM Duty | Y | Y | Y | 3 | PWM 変調 |
| Wave Morph | Y | Y | Y | 3 | 音色モーフィング |
| Pan | Y | Y | Y | 3 | オートパン |
| Tremolo Depth | Y | Y | Y | 3 | トレモロ強度の変化 |
| Filter Drive | Y | Y | Y | 3 | 歪み量の動的変化 |
| Effect On/Off | Y | (Y) | Y | 2-3 | Depth はしきい値制御時のみ有効 |
| Delay Time Sync | Y | Y | Y | 3 | リズム同期揺れ |
| Chorus Phase/Delay | Y | Y | Y | 3 | 位相/遅延の変化 |
| LFO Amount (per route) | Y | Y | Y | 3 | 変調量そのものの変化 |
| LFO Rate (meta) | (Y) | (Y) | (Y) | 0-3 | 2 系統目の変調源が必要 |
| LFO Shape (meta) | (Y) | (Y) | (Y) | 0-3 | 波形モーフ機構が必要 |

凡例:

- `Y`: 単一 LFO 設計でも直接意味がある
- `(Y)`: アーキテクチャ次第で有効

実装上の注記（現状）:

- UI/state 側には LFO 関連の表示・選択状態があります。
- ただし DSP 側のモジュレーション行列（実際に音へ反映する経路）は未接続または未完成です。

## 実装ギャップメモ（2026-04-10）

### UI未実装モジュール（優先順）
- 複数LFOの同時管理UI（現状は1系統の編集UI）
- LFOルーティングマトリクスUI（送信元LFOと送信先パラメータの接続編集）
- 拡張モジュレーション対象UI（Pitch/Fine/PWM/Pan等）
- エフェクト処理順の並び替えUI（DLY/CHR/FLTの順序変更）
- MIC/I2S詳細設定UI（入力レベル・同期・ルーティング）

### 実装計画（短期）
1. 既存UIの安定化と保存系の回帰テスト（現在のLFO/FX/スロット）
2. Delayのみ先行実装して、パラメータ配線と体感遅延を検証
3. Chorus/FilterはDelayと同じ実装パターンで段階追加
4. 複数LFO化とルーティングマトリクスを最後に追加

### Delayテスト実装の方針
- まずはイベント駆動の簡易エコーとして実装する
- 反映対象: Delay On/Off, Time, Feedback, Mix
- 目的: UIパラメータ->音声処理経路の接続確認と、演奏時の体感評価
- 留意: 本格DSP版（サンプル単位バッファ処理）は次フェーズ

## 進捗メモ（2026-04-10 追記）

### UI
- LFOラベルをターゲットパラメータ別の On/Off スイッチとして実装
- LFOターゲット切替時に、ターゲットごとの LFO On/Off 状態を反映
- 鍵盤エリアをメモリスロット直下から下端まで拡張

### 保存機能
- パラメータ保存を 5 スロット化（M1-M5）
- LFOターゲット別パラメータ（Rate/Depth/Shape/Enabled）をスロット保存対象に追加

### オーディオ（テスト実装）
- Delay を先行実装（イベント駆動の簡易エコー）
- 反映対象: Delay On/Off, Time, Feedback, Mix
- FBK の最大反復回数を 16 回に拡張

### ビルド確認
- 変更ごとに `platformio run -v` でビルド成功を確認
- 既知の framework 警告（`periph_ctrl.h`）は継続

### 次の候補
- Chorus / Filter の実DSP反映
- 複数LFO UI とルーティングマトリクス UI
- Delay を本格DSP（バッファ処理）へ置換
